/**
 * src/utils/dom.ts
 *
 * Utilidades de dominio (sí ligadas al DOM del navegador y
 * al producto Nova).
 *
 * Distinción con src/lib:
 *   - lib  = agnóstico al proyecto (cn, math helpers, string helpers...)
 *   - utils = específicas del dominio (focus, scroll, layout Nova...)
 */

// Sprint 10.1: infraestructura vacía preparada.
export {};
