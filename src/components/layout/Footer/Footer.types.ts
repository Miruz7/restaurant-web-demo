import type { CSSProperties } from "react";

/**
 * Footer placeholder (Sprint 10.1).
 * API expuesta preparada para cuando llegue el contenido real:
 * links por columna, redes sociales, badges de pagos, newsletter...
 */
export interface FooterProps {
  readonly className?: string;
  readonly style?: CSSProperties;
}
