export type {
  BaseTypographyProps,
  HeadingProps,
  TextProps,
  CaptionProps,
} from "./Typography.types";

export type {
  HeadingLevel,
  TextSize,
  TextWeight,
  CaptionSize,
} from "./Typography.config";

export {
  HEADING_TAGS,
  HEADING_VARIANTS,
  TEXT_SIZES,
  TEXT_WEIGHTS,
  DEFAULT_TEXT_SIZE,
  DEFAULT_TEXT_WEIGHT,
  CAPTION_SIZES,
  DEFAULT_CAPTION_SIZE,
} from "./Typography.config";
